{
  "mcpServers": {
    "versade": {
      "command": "uvx",
      "args": ["--from", "versade", "versade", "mcp"],
      "env": {
        "VERSADE_LOG_LEVEL": "INFO",
        "VERSADE_TRANSPORT_MODE": "stdio"
      }
    }
  }
} 